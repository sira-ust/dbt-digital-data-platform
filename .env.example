# Copy to .env and fill in values — do not commit .env
# Local DuckDB dev needs NO credentials. These are for the future
# production target only (engine TBD — followup.md).

# Databricks (if chosen)
# DATABRICKS_HOST=
# DATABRICKS_HTTP_PATH=
# DATABRICKS_TOKEN=

# Fabric (if chosen)
# FABRIC_SERVER=
# FABRIC_DATABASE=
# AZURE_TENANT_ID=
# AZURE_CLIENT_ID=
# AZURE_CLIENT_SECRET=
